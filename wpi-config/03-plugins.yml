---

# This file is a YAML formatted file. YAML indenting is done in spaces not
# tabs, and whitespace is significant. If you don't stick to this, it will
# fail on provision

# These are your website, and their names plugins/themes they're
# located in. See the docs for how to define these, and what all the keys
# and options are
# Remember, whitespace is significant! Tabs and spaces mean different things

single:
  - name: duplicate-post
    package: wp-cli
#  - name: envato-market
#    package: wp-cli
#    ver: "*"
#    zip: https://github.com/envato/wp-envato-market/releases/download/v2.0.1/envato-market-2.0.1.zip
#  - name: bitbucket_team/repo_name
#    package: bitbucket
#    setup: custom

bulks:
  elementor-pro: "*"

mu_plugins:
  - name: mu-plugin-name
    php_url: ""

setup:
  custom:
    bitbucket:
      key: ""
      secret: ""
    composer: install
    npm:
      dev: npm run dev
      prod: npm run prod
